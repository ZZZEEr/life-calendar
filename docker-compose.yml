services:
  life-calendar:
    build: .
    container_name: life-calendar
    ports:
      - "811:811"
    volumes:
      - ./persist:/app
    environment:
      - ADMIN_PASSWORD=MyStr0ngP@ssw0rd   # 改成你自己的强密码
      - SECRET_KEY=random-secret-key-here  # 改成随机字符串
    restart: unless-stopped
